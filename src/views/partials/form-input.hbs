<div class="input-container">
   <label for="{{name}}">{{label}}</label>
   <input type="{{type}}"  name="{{name}}" id="{{name}}" class="inp-Box name" value="{{value}}">
   {{#if error}}
   <div class="error">{{error}}</div>
   {{/if}}
</div>