<div class="header-album">
    <div class="inner-cover">
    <img class="album_image_header"
        src="../images/album/{{songsDetail.artist.firstName}}{{songsDetail.artist.lastName}}{{songsDetail.album.name}}.png"
        alt="{{playlistDetail.name}}">
    </div>
    <div class="header-image">
        <img src="../images/album/{{songsDetail.artist.firstName}}{{songsDetail.artist.lastName}}{{songsDetail.album.name}}.png" alt="{{playlistDetail.name}}">
        <div class="info-album">
            <h1 class="info-album-text">{{songsDetail.name}}</h1>
            <h2 class="info-album-text">{{songsDetail.artist.firstName}} {{songsDetail.artist.lastName}}</h2>
        </div>
    </div>
</div>